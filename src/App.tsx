import Loading from "./components/loading/Loading"
import Intro from "./pages/intro/Intro"


function App() {
  return (
    <>
      {/* {width < 900 ? (
       <Intro />
      ) : (
        <Loading />
      )} */}
        <Loading />
    </>
  )
}

export default App
